# Environment variables for the TEST environment
# These values are used ONLY during automated tests

DATABASE_URL="postgresql://prisma_admin:PrismaPass123!@localhost:5432/assessment_test?schema=public"
NODE_ENV="test"
